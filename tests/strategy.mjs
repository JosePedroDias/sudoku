import tap from 'tap';

// import { record } from '../utils.mjs';
//import { Board } from '../board.mjs';
//import { Board } from '../strategy.mjs';

tap.test('', (t) => {
  // const b = new Board();

  tap.equal(1, 1);

  t.end();
});
